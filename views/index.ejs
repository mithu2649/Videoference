<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form>
        <h2>Create a Room</h2>
        <input type="text" id="room" name="room" placeholder="Enter room name here" required>
        <button id="createRoomBtn" type="submit">Create Room</button>
    </form>
    <script defer>
        document.querySelector('#createRoomBtn')
                .addEventListener('click', (e) => {
                    e.preventDefault();
                    e.stopPropagation();

                    let room = document.querySelector('#room').value;

                    window.location = window.location.href + room;
                });
    </script>
</body>
</html>